version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
    env_file:
      - .env
    restart: unless-stopped
    volumes:
      - ./logs:/app/logs

  # Future: Add database, redis, etc.
  # postgres:
  #   image: postgres:15-alpine
  #   environment:
  #     POSTGRES_DB: uniweb_ai
  #     POSTGRES_USER: admin
  #     POSTGRES_PASSWORD: ${DB_PASSWORD}
  #   volumes:
  #     - postgres_data:/var/lib/postgresql/data

# volumes:
#   postgres_data:
